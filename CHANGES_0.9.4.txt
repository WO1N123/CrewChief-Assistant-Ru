CREWCHIEF RU BUILDER 0.9.4 — PORT AND SINGLE-INSTANCE FIX

Исправлено:
- приложение допускает только один запущенный экземпляр;
- повторный запуск открывает уже работающее окно из трея вместо запуска второго процесса;
- одновременное нажатие «Запустить» из окна и меню трея больше не создаёт два MQTT-брокера;
- запуск и остановка ассистента сериализованы;
- MQTT-сервер запускается и освобождается идемпотентно;
- ошибка занятого порта теперь объясняет, что именно нужно закрыть или изменить;
- сохранены portable Python, современные полосы прокрутки и все исправления распознавания 0.9.0–0.9.3.

После сборки папка output содержит только CrewChiefRU_Setup.exe.
